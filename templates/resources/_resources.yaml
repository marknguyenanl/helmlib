{{ define "helmlib.bmachines" }}
  resources:
    requests:
      cpu: {{ .Values.cpu }}m
      memory: {{ mul .Values.memory 1 }}Mi
    limits:
      cpu: {{ mul .Values.cpu 1 }}m
      memory: {{ mul .Values.memory 1 1 }}Mi
{{ end }}
