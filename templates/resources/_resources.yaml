{{- define "helmlib.engineused" -}}
{{- $usedengine := .Values.overlay.usedengine -}}
{{- with (.Values.root).engines }}
{{/* {{- $remapengine := printf ".%s" $usedengine -}} */}}
{{- $requestscpu := tpl (printf "{{ (.%s.requests).cpu }}" $usedengine) . -}}
resources: 
  requests:
    cpu: {{ $requestscpu }}
{{- end -}}
{{- end -}}
