{{- define "helmlib.engineused" -}}
{{ $usedengine := (.Values.overlay).usedengine }}
{{ $requests := ((((.Values.root).engines).ac1l1).requests) }}
{{ $limits := ((((.Values.root).engines).ac1l1).limits) }}

{{/* {{- range $engine := ($.Values.global).engines }} */}}
{{/* --- */}}
{{/* {{- if $engine }} */}}
resources: 
  requests:
    cpu: {{ $requests.cpu }}
    memory: {{ $requests.memory }}
  limits:
    cpu: {{ $limits.cpu }}
    memory: {{ $limits.memory }}
{{/* {{- end -}} */}}
{{/* --- */}}
{{/* {{- end -}} */}}
{{- end -}}
